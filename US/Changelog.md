# Changelog

## PluginLoader
 - 1.0.2
   - Added the missing "updateURL" tag
 - 1.0.1
   - Plugin stop function is now run *after* the observer and style have been removed
 - 1.0.0
   - Initial upload

## EmbedEmotes
 - 1.1.0
   - Fixed click event listener being added to the element above the button
   - Hide the lock icon when hovering over the emote
 - 1.0.2
   - Discord changed the css class structure again for no reason
   - Added the missing "updateURL" tag
 - 1.0.1
   - Fixed a typo that caused the plugin to fail silently
 - 1.0.0
   - Initial upload

## HideTime
 - 1.1.1
   - Discord changed the css class structure again for no reason
     - Unlike with my other plugins (with the exception of the plugin loader), the previous version still works (as of this update being released), but peformance *might* be worse with it since it could check more elements than necessary
   - Added the missing "updateURL" tag
 - 1.1.0
   - Initial upload

## HideNameSize
 - 1.0.1
   - Discord changed the css class structure again for no reason
   - Also added the missing "updateURL" tag
 - 1.0.0
   - Initial upload

## HighResImages
 - 1.0.2
   - Discord changed the css class structure again for no reason
   - Added the missing "updateURL" tag
 - 1.0.1
   - Added an additional messy css selector to fix blurry zoomed images
 - 1.0.0
   - Initial upload

## VideoLoop
 - 1.2.1
   - Fixed the internal version number not matching the userscript's version number
   - Discord changed the css class structure again for no reason
   - Added the missing "updateURL" tag
 - 1.2.0
   - Initial upload
